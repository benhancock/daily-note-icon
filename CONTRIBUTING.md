# Contributing

Bug reports and focused pull requests are welcome.

## Development setup

1. Install Node.js 22 or newer.
2. Run `npm install`.
3. Run `npm run check` before opening a pull request.

Please keep changes narrowly scoped, preserve mobile-safe TypeScript even though
the tab-header feature is desktop-only, and avoid adding dependencies unless
they are necessary.
